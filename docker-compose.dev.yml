version: '3'
services:
    web-2:
        build:
            context: .
            dockerfile: Dockerfile.dev
        ports:
            - "3000:3000"
        volumes:
            - .:/app
            - /app/node_modules # Exclui a pasta node_modules do volume montado
